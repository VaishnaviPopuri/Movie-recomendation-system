import React from 'react'
// Import the JSON data from user_data.json
import jsonData from './user_data.json';

// Log the users array to the console
console.log(jsonData.users);

export default function App() {
  return (
    <div>
        <Movie/>

        </div>
  )
}
